{
  "name": "runner",
  "version": "1.0.0",
  "description": "Capsule agent runner \u2014 resolves its own ENS name, heartbeats, halts on revoke",
  "scripts": {
    "start": "tsx src/runner.ts",
    "preflight": "tsx src/preflight.ts",
    "resolve": "tsx src/resolve.cli.ts",
    "config": "tsx src/config.cli.ts",
    "prompt": "tsx src/prompt.cli.ts",
    "heartbeat": "tsx src/heartbeat.cli.ts",
    "dev:prompt-server": "tsx dev/prompt-server.ts",
    "typecheck": "tsc --noEmit",
    "dev:gateway-smoke": "docker build -t capsule-runner . && docker run --rm --network none -v \"$PWD/dev:/capsule/dev:ro\" --entrypoint node capsule-runner --import tsx /capsule/dev/gateway-smoke.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
